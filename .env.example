# .env.example
# Copy this file to `.env` and fill in real values. Do NOT commit real
# secrets to version control.

########################################
# Application
########################################
# A long, random secret key. Required for cryptographic signing.
SECRET_KEY=your_secret_key_here
# A short name for the project (display/branding)
PROJECT_NAME=your_project_name_here

########################################
# CORS
########################################
# Comma-separated list of allowed origins for CORS.
# Examples: http://localhost,http://localhost:8000,https://example.com
BACKEND_CORS_ORIGINS=http://localhost,http://localhost:8000

########################################
# MySQL / Database
########################################
# Database connection settings
MYSQL_USER=your_mysql_user
MYSQL_PASSWORD=your_mysql_password
MYSQL_SERVER=your_mysql_server
# Default MySQL port is 3306
MYSQL_PORT=3306
MYSQL_DB=your_mysql_database

########################################
# File uploads
########################################
# Directory where uploaded files will be stored by the application.
# Use an absolute path or a path relative to the project root.
FILE_UPLOAD_DIR=your_file_upload_directory

########################################
# SMTP / Email (for outgoing emails)
########################################
# Host and port for the SMTP server
SMTP_HOST=smtp.your-email-provider.com
SMTP_PORT=587
# Credentials for SMTP auth (if required)
SMTP_USER=your_smtp_user
SMTP_PASSWORD=your_smtp_password
# TLS/SSL flags: use literal True or False (no quotes)
SMTP_TLS=True
SMTP_SSL=False

########################################
# From address used when the app sends emails
########################################
EMAILS_FROM_EMAIL=your_email@example.com
# Friendly display name for outgoing emails
EMAILS_FROM_NAME=emails_from_name

# Notes:
# - After copying to `.env`, replace placeholders with real values.
# - Keep `SECRET_KEY`, DB passwords, and SMTP passwords private.
# - For boolean-like flags use `True` or `False` (case-sensitive as strings).